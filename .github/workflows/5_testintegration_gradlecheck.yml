name: (5.x) Run tests

# This workflow runs when any of the following occur:
# - On any changes

on:
  push:
    paths:
      - "src/**" # Match changes in source files.
      - "*.gradle" # Match changes in Gradle configuration.

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # TODO use branch resolver

  # Publish common-utils to Local Maven for notifications plugin
  publish-common-utils:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          repository: wazuh/wazuh-indexer-common-utils
          ref: ${{ github.ref_name }} # Use the same branch as the current workflow

      - uses: actions/checkout@v6
        with:
          repository: wazuh/wazuh-indexer-common-utils
          ref: main # Use 'main' branch if the current branch is not found in the common-utils repository
        if: ${{ failure() }}

      - name: Publish common-utils to Local Maven
        uses: wazuh/wazuh-indexer-common-utils/.github/actions/5_local_maven_publisher@main
        with:
          reference: ${{ github.ref_name }} # Use the same branch as the current workflow
      
      - name: Publish common-utils to Local Maven
        uses: wazuh/wazuh-indexer-common-utils/.github/actions/5_local_maven_publisher@main
        with:
          reference: main # Use 'main' branch if the current branch is not found in the common-utils repository
        if: ${{ failure() }}

      - name: Cache Maven Local
        uses: actions/cache/save@v5
        with:
          path: ~/.m2/repository
          key: maven-common-utils-${{ github.ref_name }}

  call-test-workflow:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 25

      - name: Get version
        id: version
        run: echo "version=$(jq -r '.version' VERSION.json)" >> "$GITHUB_OUTPUT"

      - name: Publish to Maven local
        uses: ./.github/actions/5_local_maven_publisher
        with:
          version: ${{ steps.version.outputs.version }}
          revision: '0'
          reference: ${{ github.ref }}

      - name: Run Gradle check
        run: ./gradlew check
